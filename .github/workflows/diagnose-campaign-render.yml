name: Diagnose campaign render

# Legacy exploratory workflow. The Campaign runtime PR matrix is now the
# authoritative PR check and already captures browser logs, screenshots and
# runtime evidence, so do not launch a duplicate CheerpJ browser run per commit.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use Campaign runtime PR matrix for active campaign diagnostics.'

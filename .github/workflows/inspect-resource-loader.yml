name: Inspect resource loader

# Manual-only: the campaign preparation job now dumps the bytecode needed for
# loader/runtime diagnosis, so this separate PR workflow is redundant.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Resource/campaign bytecode is collected by Campaign runtime PR matrix.'

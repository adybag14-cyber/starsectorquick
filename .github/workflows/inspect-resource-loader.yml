name: Inspect resource loader

# Kept for manual bytecode investigation only. The active campaign PR matrix
# now collects the relevant loader/campaign disassembly in its prepare job.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Campaign runtime PR matrix now owns automated bytecode diagnostics.'

import java.io.*;import java.nio.file.*;import java.util.jar.*;import org.objectweb.asm.*;
public final class VerifyCampaignGameplayProbePatch{
 static final String E="com/fs/starfarer/campaign/CampaignState.class",P="com/fs/starfarer/BrowserGameplayProbe";
 public static void main(String[]a)throws Exception{if(a.length!=1)throw new IllegalArgumentException("usage: VerifyCampaignGameplayProbePatch jar");final int[] start={0},ready={0},dismiss={0};try(JarFile j=new JarFile(Path.of(a[0]).toFile())){JarEntry e=j.getJarEntry(E);if(e==null)throw new AssertionError("CampaignState missing");try(InputStream in=j.getInputStream(e)){new ClassReader(in).accept(new ClassVisitor(Opcodes.ASM9){@Override public MethodVisitor visitMethod(int ac,String n,String d,String s,String[]x){return new MethodVisitor(Opcodes.ASM9){@Override public void visitMethodInsn(int op,String o,String n,String d,boolean itf){if(op==Opcodes.INVOKESTATIC&&P.equals(o)){if("coreTabStart".equals(n))start[0]++;else if("coreTabReady".equals(n))ready[0]++;else if("coreUiDismissed".equals(n))dismiss[0]++;}}};}},ClassReader.SKIP_DEBUG|ClassReader.SKIP_FRAMES);}}
 if(start[0]!=2||ready[0]<2||dismiss[0]!=1)throw new AssertionError("campaign gameplay hooks start="+start[0]+" ready="+ready[0]+" dismiss="+dismiss[0]);System.out.println("VerifyCampaignGameplayProbePatch: OK start="+start[0]+" ready="+ready[0]+" dismiss=1");}
}
